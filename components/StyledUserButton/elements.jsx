import styled from 'styled-components';
import { UserButton } from '@clerk/clerk-react';

export const StyledUserButton = styled(UserButton)`
        top: 2rem;
        left: 2rem;
        font-family: sans-serif;
`